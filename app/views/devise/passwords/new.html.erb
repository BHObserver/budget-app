<div class="heading">
<%= link_to(image_tag("left-arrow.svg", alt: "Back Arrow"), "javascript:history.go(-1)", class: 'back-icon') %>
<div class="empty-div"></div>
</div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <h2 class="form-title">Forgot your password?</h2>
  <div class="field">
    <%= f.label :email, class:"form-label" %><br />
    <%= f.email_field :email, class:"form-input", placeholder:"Email", autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions", class:"splash-btn" %>
  </div>
<% end %>

<div class="shared-links-container">
  <%= render "devise/shared/links" %>
</div>
